---
phase: 03-swarm-demo
plan: 02
type: execute
wave: 6
depends_on:
  - 03-01
files_modified:
  - azure.yaml
  - backend/Dockerfile
  - frontend/Dockerfile
  - infra/bicep/main.bicep
  - infra/bicep/parameters.dev.json
  - frontend/staticwebapp.config.json
  - README.md
  - scripts/demo-smoke.ps1
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - DS-07
  - DS-08
user_setup:
  - service: azure-subscription
    why: "Create the Azure resources used by the demo"
    env_vars:
      - name: AZURE_SUBSCRIPTION_ID
        source: "Azure portal or CLI profile"
      - name: AZURE_TENANT_ID
        source: "Azure portal or CLI profile"
  - service: azure-openai
    why: "Production model access for the swarm"
    env_vars:
      - name: AZURE_OPENAI_ENDPOINT
        source: "Azure OpenAI resource"
      - name: AZURE_OPENAI_API_KEY
        source: "Azure OpenAI resource"
      - name: AZURE_OPENAI_DEPLOYMENT
        source: "Azure OpenAI deployment name"
  - service: azure-ai-search
    why: "Repo grounding in the deployed environment"
    env_vars:
      - name: AZURE_AI_SEARCH_ENDPOINT
        source: "Azure AI Search resource"
      - name: AZURE_AI_SEARCH_API_KEY
        source: "Azure AI Search resource"
      - name: AZURE_AI_SEARCH_INDEX
        source: "Search index name"
  - service: azure-container-apps
    why: "Backend hosting in Azure"
    env_vars:
      - name: CONTAINER_APP_ENVIRONMENT
        source: "Azure Container Apps environment name"
  - service: azure-static-web-apps
    why: "Frontend hosting in Azure"
    env_vars:
      - name: STATIC_WEB_APP_NAME
        source: "Azure Static Web Apps resource name"
must_haves:
  truths:
    - "The backend and frontend can be deployed to Azure with documented environment variables"
    - "The demo can be rehearsed against a seeded repository and a known simple issue"
    - "The README and pitch assets explain the architecture and the live flow clearly"
  artifacts:
    - path: azure.yaml
      provides: "Azure deployment entrypoint"
    - path: infra/bicep/main.bicep
      provides: "Infrastructure definition for the swarm stack"
    - path: README.md
      provides: "Hackathon setup, architecture, and demo instructions"
  key_links:
    - from: "azure.yaml"
      to: "backend/Dockerfile"
      via: "container build and deploy"
      pattern: "container|image|docker"
    - from: "README.md"
      to: "scripts/demo-smoke.ps1"
      via: "reproducible demo instructions"
      pattern: "smoke|demo|rehearsal"
---

<objective>
Package the swarm for the hackathon: deploy the backend and frontend to Azure, document the live demo, and make the whole thing repeatable.

Purpose: the product is only impressive if it is dependable in front of judges.
Output: Azure deployment files, demo documentation, and a smoke-test/rehearsal path.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@PRDs/01-autonomous-devops-swarm.md
</execution_context>

<context>
- Deployment files should reflect the same architecture used by the runtime plans; no last-minute platform swaps.
- Demo instructions must match the actual system behavior from the live dashboard and run API.
- The README should make the project understandable in under a minute, then guide a deeper technical read.
- Keep the seeded issue and smoke path deterministic so the demo can be replayed confidently.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Azure deployment and containerization artifacts</name>
  <files>azure.yaml, backend/Dockerfile, frontend/Dockerfile, infra/bicep/main.bicep, infra/bicep/parameters.dev.json, .github/workflows/deploy.yml</files>
  <action>Describe the Azure Container Apps and Static Web Apps deployment flow, the backing infrastructure resources, and the container build outputs required by the swarm. Keep the deployment spec aligned with the PRD’s Azure choices and avoid introducing a second orchestration platform.</action>
  <verify><automated>python -m compileall backend; npm run build --prefix frontend; bicep build infra/bicep/main.bicep</automated></verify>
  <done>The deployment artifacts are syntactically valid and describe the intended Azure runtime clearly.</done>
</task>

<task type="auto">
  <name>Task 2: Write the README, seeded demo instructions, and pitch notes</name>
  <files>README.md</files>
  <action>Document the architecture, prerequisites, local run steps, demo flow, and the seeded repo strategy so a judge can understand the product before seeing code. Include a short pitch outline that matches the PRD’s 3-minute demo arc and keep the copy direct rather than marketing-fluffy.</action>
  <verify><automated>python -m compileall backend; npm run build --prefix frontend</automated></verify>
  <done>The repository explains itself and the demo can be rehearsed from the written instructions alone.</done>
</task>

<task type="auto">
  <name>Task 3: Add a smoke-test and rehearsal checklist for the demo</name>
  <files>scripts/demo-smoke.ps1, README.md</files>
  <action>Create a reproducible smoke script and a concise checklist that confirm the live pipeline, dashboard, and PR output are all working before showtime. Keep the smoke path focused on the demo’s critical path rather than trying to solve every edge case.</action>
  <verify><automated>python -m compileall backend; npm run build --prefix frontend; bicep build infra/bicep/main.bicep</automated></verify>
  <done>The demo can be rehearsed, checked, and repeated with a single documented workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local build artifacts → Azure deployment | Build output is promoted to cloud infrastructure here. |
| README/demo instructions → operator | Written steps can cause unsafe or incorrect setup if they drift from reality. |
| smoke script → live system | Rehearsal commands can mutate the environment if not scoped carefully. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-04 | Information Disclosure | deployment manifests | mitigate | Keep secrets in env/config references only and never hardcode keys in infra files. |
| T-03-05 | Tampering | smoke/rehearsal script | mitigate | Make the rehearsal path read-only or narrowly scoped to the demo repo and clearly document side effects. |
| T-03-06 | Denial of Service | demo launch flow | accept | Rehearsal is a controlled hackathon scenario; limited load is expected and acceptable. |
</threat_model>

<verification>
- Deployment files build or validate locally.
- README and pitch notes describe the product clearly.
- The smoke path is reproducible and aimed at the demo’s critical path.
</verification>

<success_criteria>
- The project can be deployed and explained without tribal knowledge.
- The demo can be rehearsed in a repeatable way.
- The repository is ready for a showtime run, not just a code walkthrough.
</success_criteria>

<output>
After completion, create `.planning/phases/03-swarm-demo/03-02-SUMMARY.md`.
</output>